class Program
{
    static void Main()
    {
         //TODO: Uncomment the code below to pass the first stage
         Console.Write("$ ");
        if(Console.Write(validCommandsList.Contains(inputCommand));
        if(Console.Write(validCommandsList.Contains(inputCommand))
    }
}
